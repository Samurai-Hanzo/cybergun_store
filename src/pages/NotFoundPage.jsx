import React from "react";
import NotFound from "../components/NotFound/NotFound";

const NotFoundPage = () => {
    return (
        <div>
            404 <NotFound />
        </div>
    );
};

export default NotFoundPage;
